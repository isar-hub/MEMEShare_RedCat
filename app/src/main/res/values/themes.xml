<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MEMEShare" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">#FF0000</item>
        <item name="colorSecondary">#000000</item>
        <item name="colorAccent">#990000</item>
        <item name="android:statusBarColor">#000000</item>
    </style>

    <style name="Theme.MEMEShare" parent="Base.Theme.MEMEShare" />
    <style name="TransparentBottomSheetDialog" parent="Theme.Design.BottomSheetDialog">
        <!-- Make the background transparent -->
        <item name="backgroundColor">#1AFFFFFF</item>
        <!-- Optionally adjust the dialog appearance -->
        <item name="bottomSheetStyle">@style/TransparentBottomSheet</item>
    </style>

    <style name="TransparentBottomSheet" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">#1AFFFFFF</item>
    </style>
</resources>